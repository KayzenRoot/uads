# Risk and gates

Risk is LOW | MEDIUM | HIGH | CRITICAL from structured signals plus affected context, not a single keyword and not “the repo also contains auth.”

Gates are selected, not dumped. Style-only frontend work does not require Web3 fuzzing. DeFi withdrawal requires web3-unit, fuzz, invariant, and security review.
