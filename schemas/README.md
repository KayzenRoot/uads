# schemas

JSON Schema documents for Architecture Freeze v0.2 data types, plus Prompt 003 execution schemas at 0.3.0 (`execution-run`, `execution-packet`, `evidence-record`, `review-record`).

UADS by NexLabs. See `docs/` for Architecture Freeze v0.2.
